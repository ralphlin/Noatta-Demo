<h1>Sign up</h1>

<%= form_for(@user, :url => { :action => "createandregisterscan" }) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>